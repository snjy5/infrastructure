# This inventory is for running the playbook on the CI/CD runner itself
# to validate syntax and logic before provisioning real infrastructure.

[local]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
