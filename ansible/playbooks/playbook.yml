- name: Deploy web server
  hosts: all
  become: true  # Runs with sudo/root privileges

  pre_tasks:
    - name: Ping my hosts for testing
      ansible.builtin.ping:

    - name: Print message
      ansible.builtin.debug:
        msg: "Connected successfully!"

  roles:
    - nginx
    - wireguard
    - firewall
