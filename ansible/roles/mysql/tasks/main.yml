---
- name: Install MySQL Server
  ansible.builtin.apt:
    name: mariadb-server
    state: present
    update_cache: yes

- name: Ensure MySQL service is running and enabled
  ansible.builtin.service:
    name: mariadb
    state: started
    enabled: yes
