---
- name: Install MySQL Server
  ansible.builtin.apt:
    name: mysql
    state: present
    update_cache: yes

- name: Ensure MySQL service is running and enabled
  ansible.builtin.service:
    name: mysql
    state: started
    enabled: yes
