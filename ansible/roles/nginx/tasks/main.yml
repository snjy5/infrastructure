---
- name: Deploy selected Nginx site configuration
  ansible.builtin.template:
    src: "{{ service_type }}.conf.j2"
    dest: "/etc/nginx/nginx.conf"
    owner: root
    group: root
    mode: '0644'
  become: true
  notify: Reload nginx

